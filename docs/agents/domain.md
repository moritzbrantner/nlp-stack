# Agent domain context

Read `CONTEXT.md` for the NLP boundary, ADR 0012 for ecosystem ownership, and
`docs/AGENT_DRIVEN_RELEASES.md` for release control. The ownership map is the
machine-readable package authority; the release plan is deliberately
non-publishing bootstrap inventory.
